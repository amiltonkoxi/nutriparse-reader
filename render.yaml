services:
  - type: web
    name: nutriparse-reader-api
    runtime: docker
    region: frankfurt
    plan: free
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    healthCheckPath: /health
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://nutriparse-reader.vercel.app,http://localhost:3000
